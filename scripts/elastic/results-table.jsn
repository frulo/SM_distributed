input {
      jdbc {
        jdbc_driver_library => "postgresql-9.4-1204.jdbc42.jar"
        jdbc_driver_class => "org.postgresql.Driver"
        jdbc_connection_string => "jdbc:postgresql://localhost:5432/sm"
        jdbc_user => "sm"
        jdbc_paging_enabled => false
        statement => "SELECT * from results_table;"
      }
}

output {
    elasticsearch {
        action => "index"
        hosts => "localhost:9200"
        index => "annotation"
        workers => 1
    }
}
